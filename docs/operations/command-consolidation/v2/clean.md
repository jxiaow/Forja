# `forja clean`

[? ????](index.md)

**??**?????????? Qt ? SDK ?????????????????

**??**?
```
forja clean [--plan] [--json]
```

**????**?workroot ????? active target?

## ??

### Qt ????

1. ?? Makefile ????
2. ?? ? ?? `make clean`?? `jom clean`?
3. ??? ? ??????????????

### SDK ????

1. Windows: ?? MSBuild `/t:Clean`
2. Linux: ?? `make clean`

### ????

?? `executeRemotePlan` ????? `forja clean --project <path> --json`?

## Result

```ts
interface CleanResult extends ForjaJsonResult {
    action: 'clean';
    plan?: CommandPlan;
}
```

## ????????

| ?? | ?? |
|------|------|
| `forja build` | clean ?? build ?? |
| `forja build fresh` | ??? clean + build |

## ???

- `forja clean --json` ??????
- `forja clean --plan --json` ???????????
- ? Makefile ????
