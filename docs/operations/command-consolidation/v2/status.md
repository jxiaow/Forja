# `forja status`

[? ????](index.md)

**??**????????????? readiness???????? target????????????????????? SSH ??????????????????

**??**?
```
forja status [--json]
```

**????**???workroot ?????? "not initialized" ??? `forja init`?

## ??

1. ?? workroot ????
2. ?? active target?? workspaceStore?
3. ????? readiness?
   - **target**???? active target
   - **toolchain**???????????? active target ? toolchain ?????
   - **sync**?sync ???????? runAt=remote ????
   - **remote**?remote ???????? runAt=remote ????
   - **runtime**??????????
4. ?????????
5. ?? nextAction??? readiness ? diagnostics?

## Readiness ??

| ?? | ?? |
|------|------|
| `ready` | ????? |
| `configured` | ?????????? |
| `not-selected` | ????target/sync/remote? |
| `missing` | ?????? |
| `blocked` | ???????? |
| `unknown` | ???? |

**ok ??**??? readiness ??? `blocked` ? `missing` ? `ok: true`?target ? `not-selected` ? `ok: false`?

## Result

```ts
interface StatusResult extends ForjaJsonResult {
    action: 'status';
    readiness: Readiness;
    remote?: RemoteStatusSummary;
    sync?: SyncStatusSummary;
    runtime?: RuntimeState;
    nextAction?: string;
    choices?: Array<{ label: string; command: string; description: string }>;
}

interface Readiness {
    target?: ReadinessState;
    toolchain?: ReadinessState;
    sync?: ReadinessState;
    remote?: ReadinessState;
    runtime?: ReadinessState;
}
```

## nextAction ????

| ?? | nextAction |
|------|-----------|
| workroot ??? | `forja init` |
| ? active target | `forja use target` |
| ????? | `forja use target --qt <path>` ? `forja list env` |
| ??????? sync server | `forja remote set --server <name>` |
| ????? sync ?? remote path | `forja remote set --remote-path <path>` |
| ???? | `forja build` |

## ????

**workroot ???**?
```json
{
    "ok": false,
    "action": "status",
    "readiness": { "target": "unknown", "toolchain": "unknown" },
    "diagnostics": [
        { "level": "warning", "message": "Not initialized, no config found" }
    ],
    "nextAction": "forja init"
}
```

**? active target**?
```json
{
    "ok": false,
    "action": "status",
    "readiness": { "target": "not-selected", "toolchain": "unknown" },
    "diagnostics": [
        { "level": "warning", "message": "No active target selected" }
    ],
    "nextAction": "forja use target"
}
```

**????????**?
```json
{
    "ok": true,
    "action": "status",
    "workspace": "C:\\Code\\myapp",
    "activeTarget": {
        "kind": "qt",
        "project": "app/app.pro",
        "mode": "debug",
        "arch": "x64",
        "runAt": "local"
    },
    "readiness": {
        "target": "ready",
        "toolchain": "ready",
        "sync": "not-selected",
        "remote": "not-selected"
    },
    "nextAction": "forja build"
}
```

**???????**?
```json
{
    "ok": true,
    "action": "status",
    "runtime": {
        "running": true,
        "pid": 12345,
        "executablePath": "C:\\Code\\myapp\\app\\debug\\app.exe",
        "logFile": "C:\\Users\\dev\\AppData\\Local\\Temp\\forja-logs\\app.log"
    },
    "nextAction": "forja stop"
}
```

## ???

| ?? | ?? |
|------|------|
| active target | workspaceStore?`getActiveTarget`? |
| ????? | active target ? toolchain ???workspaceStore? |
| sync ?? | settingsIO?`loadSyncSettings`? |
| remote ?? | settingsIO?`loadRemoteSettings`? |
| ????? | `.forja/sync-state.json`?`readRunState`? |
| ????? | `~/.forja/servers.json`?`getServerById`? |

## ????????

| ?? | ?? |
|------|------|
| `forja init` | workroot ????? nextAction |
| `forja use target` | ? active target ?? nextAction |
| `forja build` | ?????? nextAction |
| `forja stop` | ??????? nextAction |
| `forja doctor` | ?????status ????? |
| `forja list targets` | ???? targets |

## ???

- `forja status --json` ? workroot ?????? `nextAction: "forja init"`
- `forja status --json` ???????? `ok: true` ? `nextAction: "forja build"`
- ?????? workspaceStore ? activeTarget ????? settingsIO?
- ??????? sync ? remote readiness
- ????????? PID ????????
